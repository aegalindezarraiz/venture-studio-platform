apiVersion: apps/v1
kind: Deployment
metadata:
  name: growth-engine
  namespace: venture-studio
  labels:
    app: growth-engine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: growth-engine
  template:
    metadata:
      labels:
        app: growth-engine
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8007"
    spec:
      containers:
        - name: growth-engine
          image: ghcr.io/aegalindezarraiz/growth-engine:latest
          ports:
            - containerPort: 8007
          envFrom:
            - configMapRef:
                name: venture-studio-config
            - secretRef:
                name: venture-studio-secrets
          env:
            - name: PORT
              value: "8007"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8007
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8007
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: growth-engine
  namespace: venture-studio
spec:
  selector:
    app: growth-engine
  ports:
    - port: 8007
      targetPort: 8007
  type: ClusterIP
