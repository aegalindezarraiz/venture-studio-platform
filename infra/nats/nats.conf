# NATS JetStream configuration para AI Venture Studio
port: 4222
http_port: 8222

jetstream {
  store_dir: "/data"
  max_memory_store: 256MB
  max_file_store: 1GB
}

# Streams que deben existir al arrancar
# Se crean vía scripts/bootstrap.sh
